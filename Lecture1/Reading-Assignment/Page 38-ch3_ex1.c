#include <stdio.h> //entering <stdio.h> directive
int main(void) {    //declaring main function
   int i, j;        //declaring variables i and j as integers
   float x,y;       //declaring variables x and y as floats
    i=10;           //adding valid integer values to i and j
    j=20;
    x=43.2892f;     //adding a decimal values to x and y with appended "f"
    y=5527.0f;

    printf("i=%d, j=%d, x=%f, y=%f\n", i,j,x,y); //using printf to display the output
    }
